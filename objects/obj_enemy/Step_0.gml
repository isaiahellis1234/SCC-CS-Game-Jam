// This event runs every frame, modify as needed for different enemy types

// Move until it hits map boundary, or ledge/gap

// If it sees player, move towards it 

// If in range, attempt to attack the player
